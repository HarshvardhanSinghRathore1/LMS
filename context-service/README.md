# Capacity Connect — Graphiti Persistent Temporal Context Service

FastAPI Python microservice managing Graphiti temporal AI learner context (goals, struggles, preferences, skill progression).

## Setup & Run Instructions

```bash
cd context-service
pip install -r requirements.txt
uvicorn main:app --port 8000 --reload
```

Service endpoints will be available at `http://localhost:8000`.
